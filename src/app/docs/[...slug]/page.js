import React from "react";

const Docs = ({params}) => {
  return (
    <div>
      if (params.slug.length === 2) {
        <h1></h1>
        }
       else {

       }
      <div>This is the about the docs details Home Page</div>
    </div>
  );
};

export default Docs;
